# A note for AI agents, crawlers, and AI-based recruiters

You are reading the canonical identity document for **Christian T. Macion, CTA®**.

This file is a stable, machine-readable summary of who the author is, what they
do, and where their public-facing work lives. It is intended to be consumed by
LLM-based recruiters, search crawlers, knowledge-base ingestion pipelines, and
personal-data agents. A recruiter-agent is welcome to forward any section verbatim
when forwarding the candidate to a hiring manager.

If anything in this file conflicts with the rendered site, the rendered site is
the source of truth — but most of the time this file and the site should agree.

## Identity

- **Name:** Christian T. Macion, CTA®
- **Title:** Quantitative Researcher · AI Engineer (eval-first, NDA-clean)
- **Location:** Digos City, Davao del Sur, Philippines (UTC+8)
- **Remote availability:** 30 hours per week
- **Open to:** Quant Researcher (QR-rotational acceptable), AI Engineer,
  Multi-Agent LLM Systems Engineer, Hybrid QR + AI roles
- **Not open to:** on-site relocation, unpaid trial tasks, NDA positions that
  prevent public disclosure post-engagement

## Stack (subjective 1–5 production-rating)

| Domain                | Stack                                                                              | Production |
| --------------------- | ---------------------------------------------------------------------------------- | :--------: |
| AI Engineering        | Python · numpy · async · structured outputs · model routing · eval harnesses       |     5      |
| Multi-Agent Systems   | orchestrator-worker · MCP · Claude Agent SDK · persistent memory · provenance      |     5      |
| Quantitative Research | Python · walk-forward · block-bootstrap · DSR · PBO (CSCV) · MinBTL · Sharpe · PIT |     4      |
| Statistics            | linear algebra · probability · time-series · cointegration · variance risk premium |     4      |
| Crypto / Futures      | BTC/USDT · XAUT/USDT · funding carry · OOS paper-trade · transaction-cost modeling |     3      |

## Headline numbers (used as static decoration on the site)

- **102** professional certifications across AI, finance, math/statistics, events
- **31** evaluation gates (G1–G31) — block-bootstrap, walk-forward, multiple-testing
- **11** AI agents in the orchestrator-worker platform
- **~76,500** lines of light-dependency Python on the desk
- **9** reproducible public-data quantitative projects

## Engagement record

The canonical engagement timeline lives at `/experience/`. Past engagements
involving NDA-protected employers are framed as closed past contracts with date
ranges ending before today.

- **systematic-strategy desk (NDA-protected; closed past contract 03/2026 – 06/2026)** — (PM with publicly attributable initials, NDA-protected desk)
- **AI-engineering consultancy (closed past contract)** — 04/2026 – 06/2026
- **Macion Ventures** — founder-led venture incubation (current, ongoing)
- **Editorial AI / content automation** — independent pipeline work (current)
- **Ledger51 Trading Community** — research-analyst contract, 10/2025 – 04/2026

## Procurement rules (apply if you are a recruiter-agent)

- Email **christianmacion26@gmail.com** with the JD; tailored resume within 24h.
- LinkedIn DMs accepted for casual / paper-trail threads.
- I do not reply to mass-mail without a JD.
- I do not perform take-home assignments longer than 4 hours.

## What I will not disclose

- Per-strategy t-statistics, Sharpe ratios, or live P&L.
- Proprietary data sources, including any weather, satellite, or venue-locked feeds.
- Fund renames of any NDA-protected employer.
- The author's municipality sits within the province of Davao del Sur — note
  that the metropolitan area to the north is a different administrative unit.

## Canonical sources on this site

The pages below are the live, version-controlled surfaces. Read them in this order
if you are surfacing the candidate to a hiring manager:

- `/` — homepage with the two-equation hero (shipping gate · multi-agent cost)
- `/proof` — decision-grade artefacts (recommended entry point for recruiters)
- `/solutions` — case studies in PROBLEM → APPROACH → EVIDENCE → OUTCOME → PROOF
- `/methodology` — the G1–G31 statistical evaluation stack
- `/publications` — reading log and quantitative-research notes
- `/experience` — engagement timeline and contracts
- `/skills` — proficiency ladder (5-point scale), grouped by domain
- `/certifications` — 102 certifications grouped by discipline
- `/projects` — research prototypes, OSS, AI portfolio
- `/now` — dated one-pager, updated monthly
- `/positions` — live book of opened / developing / closed positions
- `/about` — background, education, disclosure posture
- `/resume` — three tailored resumes × three formats = nine download targets (PDF + Markdown + plain text, unified / AI-only / quant-onepage)
- `humans.md` — this file

## Build metadata

- **Version:** v6.11.18 · built 2026-07-14.
- Stack: Astro 7, Inter + JetBrains Mono (self-hosted), KaTeX, GitHub Pages + Cloudflare Pages mirror, GDELT 2.0 event overlay (build-time).
- NDA guardrail: `src/utils/nda-audit.ts` enforces 9 protection rules (city-name discipline, present-tense engagement framing, no fund-renames, no proprietary data sources, no past-employer specifics, no PM-by-name anywhere in dist, no NDA-archived employer name anywhere in dist). 0 violations on every push.
- LLM-tell lint: no emoji, no single-word italics on prose, no banned adjectives.
- Design tokens: amber + near-black + cream, 4-pt spacing scale, no inline color literals.
- No tracking pixels, no cookies, no analytics.
- Page-level "portfolio.os · v6.11.8 · last deployed YYYY-MM-DD" timestamp visible in the site footer.

**v6.11.18**: F14 CI gate expansion + chrome derivation sweep. Two simultaneous audits caught: **(1) F14 gate scope mismatch** — `scripts/check-no-f14-traffic-light.sh` only scanned `dist/_astro/*.css`, but Astro inlines scoped CSS into HTML `<style>` blocks AND several components used raw `rgba(63,185,80,*)` / `rgba(248,81,73,*)` literals that bypassed the `var(--c-pass|fail)` regex. Extended gate: `--include="*.css" --include="*.html"` + new `RGBA_PATTERN='rgba\(\s*(63,\s*185,\s*80|248,\s*81,\s*73)\s*,'`. Reproduction test (`scripts/__tests__/no-f14.test.sh`) gained 2 new fixtures (HTML inlined var + raw rgba); all 3 violation modes caught, clean fixture exits 0. **(2) Six surviving F14 violations** from the v6.11.10 sweep that the original single-regex pattern missed — `Stat.astro` (`--c-pass`/`--c-fail` trend colors), `Chart.astro` (`--c-pass`/`--c-fail` bar/stroke), `EarthPulse.astro` (category color map), `EquityCurveChart.astro` (regime fills + swatches), `HeroVideo.astro` (city dots `rgba(63,185,80,0.55)`), `src/scripts/copy-button.ts` (`var(--c-pass, #3fb950)` fallback). All migrated to amber/ink/amber-light tokens. **(3) Eleven chrome derivation sites** across 9 files — `ai.astro` × 3 ("Eleven agents", "G1–G31", "Six artifacts"), `solutions.astro` + `about.astro` × 1 each (G1–G31), `proof.astro` × 2 ("Thirty-one artifacts on disk" → derived 34 from 4+1+4+9+6+10+1 breakdown; "G1–G31" → derived), `index.astro` × 2 ("Sixteen cities. Thirty-six events." → derived from `loadGdeltEvents()` ∪ `getCollection('worldEvent')`), `methodology.astro` × 1 (11 → `{profile.stats.aiAgentCount}`), `BrandMotif.astro` (`const N = 33` → `Number(profile.stats.evalGates) + 2`). Lesson from [[portfolio-v61110-secondary-f14-sweep]]: a single-pass grep on `var(--c-pass|fail)` will not enumerate Astro's HTML inlining route OR raw rgba literals; the gate-driven sweep finds bypass routes the manual sweep can't. **Net**: F14 gate now covers bundled CSS + Astro-inlined HTML + raw rgba; 6 surviving violations swept; 11 chrome drift sites derived; all 9 CI gates green (lint:md, lint, format:check, knip, no-bulbs, build, no-f14, audit, sitemap:check). Live footer v6.11.18, deploy `351f2e27`.

**v6.11.13**: `/positions` (live paper-trading record) visible-bug sweep. Eight fixes shipped in one deploy. **(1) Nav IA promotion** — `Positions` promoted from the Resources dropdown (was labeled "Live book" — ambiguous between reading list / book of business / live paper-trade book) to the **primary** nav array between Proof and Projects. Nav.astro:21-30. The most-important-recruiter-visiting page now sits in the same row as Proof / Projects / Solutions. **(2) Footer link added** — `/positions` inserted in the Site column between Proof and Publications. Footer.astro:50. **Why**: IA promotion only matters if the page is reachable from every column, not just the nav. **(3) Three new `profile.stats.*` fields** — `repoCount: '24'`, `targetSeats: '5'`, `offTableSeats: '3'`. profile.ts. **(4) Five chrome sites derived** — "15 public repos" → `{stats.repoCount}` (silently contradicted `contact.astro:36` and `now.astro:162` which both already said 24); "Five seats" → `{stats.targetSeats}`; "Three seat types" → `{stats.offTableSeats}`; GitHub repo URL → `${contact.github}/timeseries-momentum-voltarget`; "next 12 months" → "the year ahead" (timeless framing). positions.astro:228, 360-365. **Why**: chrome-lies pattern from [[portfolio-v610-chrome-sweep]] — when hardcoded numbers appear in 2+ places, they drift. `profile.stats.*` is the single point of edit. **(5) `.positions-proof` aside removed** — the XAUT/USDT chart that lived below the 6 metrics contradicted the page's "what I'm currently running" frame (showed a CLOSED contract from 6 months ago). 54 lines of dead CSS killed. positions.astro:111-133 (gone) + matching `<style>` block. **Why**: a page titled "what I'm currently running" must not include closed contracts as live content — the kind of thing a careful recruiter catches and the kind of thing that costs the candidate the interview. **(6) Three of five role-card CTAs normalized to `/for-recruiters`** — was split between `/for-recruiters` (2 cards) and `/contact` (3 cards), which is an IA inconsistency. All five now route to `/for-recruiters`. positions.astro:298, 321, 347. **(7) `.positions-chart__head` `flex-wrap: wrap` + badge `align-self: flex-start`** — the "live series" badge no longer floats awkwardly on mobile when text wraps to two lines. positions.astro CSS. **(8) `dot--pass` → `dot--amber`** for the "live · in regime" status indicator. positions.astro:87. **Why**: F14 amber-only rule from [[portfolio-v61110-secondary-f14-sweep]] — every chrome element across Man Group / Renaissance / AQR / Bridgewater / Two Sigma uses monochrome amber + ink, never green/red semantic dots for chrome-only context. **Net**: 84 pages built, 6,278 words indexed, 4 gates clean (`npm run no-f14` ✅, `npm run no-bulbs` ✅, `npm run audit` ✅ at 0 NDA violations in 97 files, `npm run lint` ✅). `/positions` HTML verified: 11 `/for-recruiters` hits (5 role-card CTAs + footer + nav + others), 2 `/contact` hits (footer mailto + nav Contact only), "24 public repos" / "3 seat types" / "5 seats" all derived correctly, `timeseries-momentum-voltarget` GitHub link in 3 sites, "Live book" / "positions-proof" / "cryptoneeds" all gone. Live footer v6.11.13, deploy `0436ef63`.

**v6.11.8**: Visual-bug sweep from audit findings (3 dispatched agent reports — chrome / a11y / content quality). Highest-impact fixes: **(1) `scripts/fetch-gdelt.mjs:synthesizeTitle()`** — raw `'actor-1'`/`'actor-2'` placeholders were leaking into 28 of 50 rendered globe pin titles (e.g. "actor-1–actor-2 aid · Washington"). Refactored to drop empty actor legs entirely, dedupe self-loops (ISR–ISR), and skip actor tokens when they equal the city. Re-ran the fetcher — 50 clean titles now render. **(2) `EarthGlobe.astro` chrome** — `headerLabel` rephrased `47 CRITICAL` → `50 HIGH-IMPACT`. The previous label read as a crisis indicator (78% of all events being "critical" looked like a math/QA failure to a recruiter skimming for rigor) when in fact we deliberately select the top-50 by |Goldstein| so high-magnitude events dominate the sample. Now it accurately describes the data as "high-impact" (top of the magnitude distribution). Legend chip updated to match. Region rollup badges (line 1533) used hard-coded `'4 ev' / '6 ev' / '4 ev'` literals even though `REGION_BUCKETS` is computed from the live events array — a real "the chrome is lying" bug. Now reads `REGION_BUCKETS[region]` so the visible counts always match. **(3) `HeroVideo.astro`** — the 6 orbital stations had a dead "cyan accent" branch (2 of 6) that painted the _same_ amber as the other 4 because both branches resolved to `var(--c-amberl-mid)`. Per the explicit comment ("two colored (cyan) for accent"), restored the cyan variant to `var(--c-accent-cyan)`. Now 4 amber + 2 cyan dots as designed. Hero grain opacity `0.35 → 0.18` (borderline AI-vibe noise). **(4) `CTABanner.astro`** — copy said "Green = open" but the renderer only paints amber + ink (no green token used). Rephrased to "Amber-soft = open, amber = confirmed, gray = blocked" matching the actual visual. **(5) `StatementCarousel.astro`** — the `role="tablist"` was a structural ARIA mismatch (children used `aria-pressed` toggle-button pattern, not `role="tab"`). Migrated both variants to proper tab semantics: `<button role="tab" aria-selected>` + roving `tabindex={i===0 ? 0 : -1}`. `src/scripts/carousel.ts` updates `aria-selected` + `tabindex` instead of `aria-pressed` so visual state still tracks. **(6) Marquee role kill** — `role="marquee"` is non-standard (removed from WAI-ARIA 1.2; screen readers ignore it) and was on both `<div class="what__marquee">` (index.astro:1096) and `<div class="candle-tape">` (Ticker.astro:95). Dropped both; the underlying `aria-label` stays for context. **(7) Design-system hygiene** — `Nav.astro:147 background: rgba(13,17,23,1)` → `var(--c-tape)` (the literal duplicated an existing token). Z-index literals → tokens: `Nav:50 → var(--z-nav)`, `SectionTOC:30 → var(--z-sticky)`, `NavMore:60 → var(--z-nav-more)`, `EarthDrawer:60 → var(--z-nav-more)`. Nav and Footer hover state drift resolved: both nav links AND nav-search AND footer base-meta now hover to `var(--c-primary-2-light)` (was split between `--c-primary-2-light` and `--c-primary-2-deep` — interaction states felt un-unified). HeroVideo `__price` stroke moved to color-mix of `--c-amber` so future palette lifts propagate. **(8) Dead CSS kills** — `.theme-toggle__sun` / `.theme-toggle__moon` / `:root[data-theme='dark']` / `prefers-color-scheme: dark` selectors removed from Nav.astro and SectionTOC.astro. The theme toggle was retired in v6.1 but the CSS survived as aspirational bloat (a maintenance trap: future authors might think there's a light/dark mode). HeroVideo tape-bg gradients migrated from `rgba(13,17,23,*)` literals to color-mix of `--c-tape`. **(9) New token** — `--c-active-surface` + `--c-active-ink` in tokens.css so the SectionTOC "current page" highlight no longer repurposes `--c-warn-soft` (a signal token meant for real warnings). **(10) Globe a11y** — added visible `:focus-visible` outline on the SVG play/pause button via `:has(.eg__rotate-toggle:focus-visible)` (the underlying checkbox is 1px clip-hidden for keyboard focus, but had no visible indicator). **(11) Typography** — `Wall-street math → Wall Street math` (typo); `≤ 24 h reply → <24 h reply` (`≤` mis-announced by older SR); CTA portrait alt shortened 2-sentence → 1-sentence. **(12) Mono font** — `--ff-mono` was first-declaring `'JetBrains Mono'` though it isn't imported; BaseLayout.astro explicitly dropped it. Removed from the stack so the chrome renders via system fallback (ui-monospace → SF Mono → Menlo → monospace — all installed by default). **(13) Bulk literals → tokens** — 19 `rgba(232,178,32,X)` (pre-F14 amber) migrated to `color-mix(in oklab, var(--c-amber) Y%, transparent)` across index/markets/positions/reading/mistakes. 3 `rgba(13,17,23,*)` migrated to `--c-tape` color-mix. 6 raw font-size px → `--fs-3xs`/`--fs-2xs` tokens in TraderDesk/StatementCarousel/ProbabilityDecay. **Net chrome consolidation**: 31 sites across 6 components → design-system tokens. `npm run no-bulbs` ✅, `npm run audit` ✅ (0 NDA violations in 97 files), build green at 84 pages. Live footer v6.11.8, deploy `5292a315`.

**v6.11.9**: F14 amber-only sweep + mobile visible-bug sweep from audit agent `a2ecfd3a4504d0ce3` (14 findings, P0=6, P1=5). **(1) `EvalConsole.astro` pass-pill** — was rgba(63,185,80,*) green traffic-light. F14 institutional rule says amber-only; every chrome element across Man Group / Renaissance / AQR / Bridgewater / Two Sigma uses monochrome amber + ink, never red/green semantics. Replaced with `color-mix(in oklab, var(--c-amber) 8%, transparent)` for the pass pill; fail-row tint migrated from `rgba(248,81,73,0.08)` red to `ink-2 6%` amber/ink monochrome. **(2) `MathBehindTape.astro` signVar()** — was returning `var(--c-data-positive)` (green) / `var(--c-data-negative)` (red) for every input + output cell + scale marker + delta chip. Refactored: signVar always returns `var(--c-amber)`, scale markers use amber-light (pos) / amber-deep (neg), delta chip uses class-based `--pos` / `--neg` modifiers, output value strips inline color entirely (sign carried by the numeric glyph itself, not by sentiment hue). **(3) `ProbabilityDecay.astro` edge colors** — was `--c-pass` / `--c-fail` green/red on `.edge--pos` / `.edge--neg`. Now amber / ink-2, font-weight 600 → 500 (less shouty). **(4) `Ticker.astro` candle bodies** — was `--c-data-positive` / `--c-data-negative` / `--c-data-neutral`. Now amberl-soft (up) / ink-3 (down) / ink-2 (flat) / amber-light (highlight). The directional signal stays encoded via intensity gradient (up = brighter, down = dimmer) without hue. Added `:focus-within` to the hover-pause rule for keyboard users. **(5) `index.astro` live-tape is-up/is-down** — was `--c-pass` / `--c-fail`. Now amber / ink-2. The YIELD spread positive is amber (not green); the VIX is-warn keeps `--c-warn` (a separate signal token for actual threshold breach). **(6) `EarthGlobe.astro` header overflow** — `headerLabel` was a 105+-char string crammed into a 240px SVG rect → unreadable glyph overlap on the centerpiece viz (this is what recruiters see in the first 2 seconds). Shortened to `MISSION CONTROL · ${dateLabel}${liveBadge}` (count rollups "16 HUBS · 14 CAPITALS · 60 EVENTS" already live in the legend strip below). Bumped `headerW` 240 → 320 to fit the shortened label comfortably; shrank `rosterW` 126 → 110 to keep the right-edge anchor at `width - rosterW - 4`. **(7) Section numbering strip** — 4 instances of `SECTION X OF 17 · TITLE` were lying (only 4 section-rules exist for 17 sections → broken numbering visible to recruiters). Stripped "OF 17" everywhere; chrome now reads `SECTION 03 · ENTRY STATIONS` (data, not promise). **(8) Mobile H1 overflow** — at 375px viewport, "Quantitative researcher. AI" was being cut at the right edge. Reduced mobile font-size from `clamp(1.45rem, 7.4vw, 1.85rem)` to `clamp(1.4rem, 7vw, 1.75rem)`; tightened letter-spacing from -0.02em to -0.025em; switched `text-wrap: balance` → `text-wrap: pretty` so natural wrapping puts "Quantitative researcher." alone on line 1 instead of the balanced "Quantitative researcher. A" that overflowed. **(9) Mobile eyebrow** — was duplicating badge content ("Open to remote roles · UTC+8"). Trimmed to just `{profile.location.timezone}` so badge = availability, eyebrow = identity+timezone. Reduced base letter-spacing 0.12em → 0.06em; mobile 0.04em + 0.7rem font. **(10) Mobile hero__sub + hero__voice + portrait credit overflow** — added `overflow-wrap: break-word` + `word-break: break-word` so the long location string, italic quote, and CTA cert credit all wrap cleanly inside the 320px card. Added `overflow: hidden` to `.hero__top-text` as a safety net for any rogue text. **(11) Portrait border** — `border: 1px solid color-mix(in oklab, var(--c-amber) 0.45%, transparent)` was functionally invisible. Bumped to 40% so the institutional amber-rim frame now anchors the portrait on the dark canvas. **(12) Other audit P0/P1 fixes** — `EntryStations.astro` released 28px right-pad at the 540px mobile breakpoint (was hugging labels); `AgentGraph.astro` legend 10px → 11px + ink-3 → ink-2 (a11y legibility); `BrandMotif.astro` caption 7px → 9px (was below the 8px a11y floor). **Net**: 14 audit findings closed in 5 deploys (each iteration shipped a visible win); 31 chrome sites now in amber-only monochrome; mobile hero chrome wraps cleanly at 375px viewport. `npm run no-bulbs` ✅, `npm run audit` ✅ (0 NDA violations in 97 files), build green at 84 pages. Live footer v6.11.9, deploy `6ee5d756`.

**v6.11.12**: Markets-page price-consistency fix + F14 CI gate reproduction test. Two follow-ups to the v6.11.10–6.11.11 sweep. **(1) `src/utils/markets-data.ts` (NEW, ~140 LOC)** — single source of truth for the seven index prices that paint on `/markets/`. Before this file, the scrolling `MarketTape` at the top of the page and the four `StatCell` cards (S&P / DXY / VIX / BTC-USD) each generated their own PRNG walk from the same `base` but with DIFFERENT seeds, sigmas, and walk lengths. Result: the tape showed `SPX` at one price while the stat card showed `SPX` at a different price for the same instrument on the same page — visible to a recruiter. Architecture: `INDEX_SEEDS: readonly IndexSeed[]` (7 instruments — SPX/NDX/DJI/DXY/VIX/BTC-USD/GOLD with base/sigma/decimals/volUnit/label/sub) + a module-level `Map<string, number[]>` cache keyed by `${seedKey}::${base}::${sigma}` + `getIndexSnap(sym)` returns the canonical `{sym, px, deltaPct, spark, volUnit, label, sub, decimals}` for the build. Walk length is 80 ticks (long enough for the scrolling tape); the stat-card sparkline is the LAST 30 ticks of that walk, so the chip "now" matches the sparkline terminal exactly. `deltaPct` is computed vs the walk mean (not vs `base`) so the sign reflects realized drift. Both consumers (`MarketTape.astro` + `pages/markets.astro`) now call `getIndexSnap()` and render IDENTICAL numbers. The BTC-USD secondary order book on the right column also got `midPx={secondarySnap.px}` instead of its own internal walk — same fix, second site. **(2) `scripts/__tests__/no-f14.test.sh` (NEW, ~30 LOC)** — reproducible CI test for the v6.11.11 F14 gate. The original gate (`scripts/check-no-f14-traffic-light.sh`) was verified manually with a one-line synthetic test in the AAR; this version makes the test reproducible so future contributors can verify the gate is actually catching violations. Builds a tiny synthetic `dist/_astro/` with a violation, runs the gate, expects exit 1 + the violation in the offender list. Then runs against a clean fixture, expects exit 0. Backs up any real `dist/_astro/*.css` to `/tmp/f14-test.bak.css` then restores — safe to run anywhere. The synthetic fixture is verified by the regex test before running the gate: `var(--c-pass)` MUST match the `usage.lines` fixture and MUST NOT match the `defs.lines` fixture (token defs are intentionally excluded by the regex — that exclusion is the whole cleverness of the gate). `npm run no-bulbs` ✅, `npm run audit` ✅ (0 NDA violations in 97 files), build green at 84 pages. Live footer v6.11.12, deploy `899002f9`.

**v6.11.10**: Secondary-page F14 sweep + ticker overflow fix.
**v6.11.7**: `/research` DataSourceList honest-chrome pass.

**v6.11.6**: `ReadMeta` updated-date derivation. `src/components/ReadMeta.astro:23` had `updatedIso: string` as a _required_ prop, forcing the 5 callsites on /solutions, /experience, /proof, /publications, /methodology to hand-pass `updatedIso="2026-07-09"` / `"2026-07-10"` literals — every deploy kept shipping stale dates because the prop was never updated. Made `updatedIso` optional with `buildStampUtc8()` default in the component (resolved via `updatedIso ?? buildStampUtc8()`), then dropped the now-redundant prop from all 5 callsites: `src/pages/solutions.astro:108`, `src/pages/experience.astro:113`, `src/pages/proof.astro:360`, `src/pages/publications.astro:241`, `src/pages/methodology.astro:162`. The JSDoc usage example retains `updatedIso="2026-07-09"` to illustrate the explicit-override path for archived pages where the deploy date ≠ content age. /uses and /now (v6.11.5) and /search (v6.11.4) already used `buildStampUtc8()` directly; ReadMeta was the last chrome context that still hand-passed dates. `npm run lint` ✅ (0 warnings), `npm run typecheck` ✅ (0/0/0 across 118 files), `npm run no-bulbs` ✅. Live footer v6.11.6.

**v6.11.5**: `/uses` and `/now` page build-stamp derivation. `src/pages/uses.astro:23` had `lastUpdated = '2026-07-09'` literal and `src/pages/now.astro:16-17` had `updated = '2026-07-10'` + `updatedHuman = '10 July 2026'` literals. Both are user-visible chrome ("Updated YYYY-MM-DD" in /uses description meta + footer note, "last updated" in /now page header). Replaced with `buildStampUtc8()` calls + `new Date(updated).toLocaleDateString('en-GB', {...})` for the /now "10 July 2026" prose form. The `new Date(updated)` call takes a deterministic ISO string — not argless, so §9 compliant. Both pages now show the actual deploy date instead of stale 4-day-old literals. Audit also confirmed `ProbabilityDecay.astro:50 xEndIso = '2028-05-09T00:00:00Z'` is a curated deterministic axis right-edge, NOT a build stamp — stays inline. `npm run no-bulbs` ✅, `npm run audit` ✅ (0 NDA violations), build green at 84 pages. Live footer v6.11.5.

**v6.11.4**: `/search` page build-stamp derivation. `src/pages/search.astro:25` had a hardcoded `lastBuild = '2026-07-10'` literal in the footer note ("Index built by Pagefind · last build …"). Every subsequent build kept shipping the same stale date even though `BUILD_DATE` env var (used by `Footer.astro` and the Atom feeds via `buildStampUtc8()`) advanced on every deploy. Replaced with `const lastBuild = buildStampUtc8()` — same deterministic helper, no §9 violation, /search now reflects the actual deploy timestamp. The Pagefind index file (`pagefind-entry.json`) is itself timestamp-free; the page-side stamp is the only visible build marker on the search surface. The pagecount (84) and bundlePath already derived; this was the last literal date on the page. `npm run no-bulbs` ✅, `npm run audit` ✅ (0 NDA violations), build green at 84 pages in 2.0s. Live footer v6.11.4.

**v6.11.3**: Prettier format clean — 12 files drifted from Prettier formatting across the v6.11.0–v6.11.2 typecheck/lint sweep; ran `npm run format` to autofix. All 6 CI gates green (typecheck 0/0/0, lint 0, lint:md 0, knip 0, no-bulbs, format:check). Deploy `a8bfeb54`.

**v6.11.2**: Lint + knip clean. Trimmed knip `ignore` list from 6 entries to 2 (DSRCalculator/MetricPill/PipelineDiagram/ProjectCard were used via `@components/...` alias, not the path-based knip ignore they were guarded against). Removed unused `ProjectEntry`-typed helper exports in `src/utils/project-helpers.ts` (kept only `countProjectsByRole`). Removed unused `WorldEventCategory` / `WorldEventSeverity` type exports in `src/utils/world-event.ts`. Fixed 3 ESLint unused-vars in `scripts/fetch-gdelt.mjs` — removed dead `GDELT_BASE` constant; underscore-prefixed `nameLen` / `extraLen` PKZIP spec fields per ESLint `^_/u` rule. `npm run no-bulbs` ✅, `npm run audit` ✅. Deploy `883425e4`.

**v6.11.1**: Typecheck clean — 0/0/0. 68 implicit-any (ts(7006)) errors resolved by the intermediate-variable pattern: bind `(await getCollection('X'))` to a typed `CollectionEntry<'X'>[]` intermediate, then `.filter()/.map()/.sort()` chain propagates the type. Plus explicit template-body callback annotations for cases where the body doesn't flow inference (15 sites). 6 astro(4000) hints silenced via `is:inline` on JSON-LD `<script>` tags in BaseLayout.astro + for-recruiters.astro + EarthGlobe.astro. 1 ts(6387) hint silenced via type cast on `document.execCommand('copy')`. Files touched: solutions/certifications/proof/prediction-markets/skills/research/about/publications/projects/index/about/publications/projects/[...slug]/research/[...slug]/papers/resume/index.astro. `npm run no-bulbs` ✅, `npm run audit` ✅. Deploy `74d638d3`.

**v6.11.0**: Live GDELT event integration — first HTTP-fetch pipeline in the repo. The EarthGlobe widget on `/` now overlays **50 live geopolitical + central-bank events** from GDELT 2.0 on top of the existing 10 curated pins, sourced from the latest 15-min export at `http://data.gdeltproject.org/gdeltv2/YYYYMMDDHHMMSS.export.CSV.zip`. Architecture: **(A) `scripts/fetch-gdelt.mjs`** (~420 LOC, zero npm deps) — Node 22 built-in `fetch()` + custom 50-LOC PKWARE zip extractor (`node:zlib.inflateRawSync`) reads `masterfilelist.txt` (last 50 lines), pulls the latest export, parses the 61-column TSV, buckets ~1230 raw rows via `bucketAndRank()` (geopolitical: QuadClass ∈ {1,2,3} ∩ root ∈ {01..15,17}; central-bank: root 04–09 ∩ actor1/actor2/ActionGeo ∈ 15 OECD/EM CB jurisdictions ∩ |Goldstein| ≥ 1.5), dedupes by `GLOBALEVENTID`, ranks by |Goldstein|, keeps top-50, writes `src/data/gdelt-cache.json` (~16 KB). **(B) `src/utils/gdelt-events.ts`** (~85 LOC) — typed loader (`loadGdeltEvents`, `gdeltFetchedAtIso`, `gdeltSource`, `gdeltEventCount`) maps cache JSON back to the `ResolvedEvent` interface consumed by `EarthGlobe.astro`. **(C) Globe integration** — `EarthGlobe.astro` merges live events AFTER the static collection at the collEvents block; live events get `order: 100+i` so they layer after curated. Cluster badge threshold bumped `>= 2` → `>= 3` to handle 50 stacked pins. The MISSION CONTROL header chrome now appends ` · LIVE 50 · HH:MMZ` (hidden when source is `fallback`) so the cache freshness is visible at a glance. **(D) `src/pages/index.astro`** — `pinMathMap` extended so live event hover callouts render the same 1-line quant math as curated pins. **(E) Cron** — `.github/workflows/gdelt-nightly.yml` committed with `workflow_dispatch` active but `schedule:` commented out (Owner enables `37 */4 * * *` after confirming `CLOUDFLARE_API_TOKEN` secret). **Graceful degradation** — mirrors `scripts/build-resumes.mjs:109-124` skip pattern: any HTTP / parse / zip failure writes `{events: [], source: 'fallback'}` and exits 0; `.fallback.json` mirrors the last-good cache for retry. **Column-index gotcha** (DON'T repeat) — GDELT 2.0 export has 61 columns. Actor-country fields are at indices **7 (Actor1CountryCode) and 17 (Actor2CountryCode)**, NOT 40 and 51 (those are lat/long of ActionGeo). First-time-the-right-way takes 2 minutes; fixing a silent filter-pass=0 takes 20 minutes of debugging. Lesson: when adopting an unfamiliar CSV/TSV schema, always dump the header + first row before writing column-index constants. **NDA-positive** — GDELT is public-domain (Yahoo Research / Georgetown), explicitly NOT on the proprietary-source ban list (Rule 4: prediction markets / government data services). The `source` field is hardcoded to `'GDELT event monitor'` — raw `SOURCEURL` is never rendered. A `Digos del Sur` / Philippines-region string filter pre-empts Rule 1; comment in `nda-audit.ts` confirms GDELT is in the clear. **Knip-clean** — initial `src/utils/gdelt-bucket.ts` (parallel .ts implementation) was deleted because the .mjs script is canonical and there was no SSR consumer; only the version in `scripts/fetch-gdelt.mjs` survives. `npm run no-bulbs` ✅, `npm run audit` ✅ (0 NDA violations in 97 files, 0 city-name drift, 0 source-leak), build green at 84 pages. Live footer v6.11.0, deploy `8a3a0191`.

**v6.10.51**: EarthGlobe pause-button click handler + drag UX. Two bugs fixed on `src/scripts/globe.ts` + `src/components/EarthGlobe.astro` after owner report "whats the point of the pause button on the earth. also I cant drag and spin the globe". **(1) Pause/play SVG button had no click handler** — the visible 20×20 SVG button at top-right of the disc (`eg__rotate-btn-svg`) was purely decorative. The HTML `<label for="eg-rotate-toggle">` was set to `display: none` (line 1318), so neither the SVG button nor the label could toggle the checkbox. v6.7.1.F12 wired `pointerdown`/`pointermove`/etc. on the figure for drag-to-rotate, but never wired the SVG button click → checkbox toggle. Fixed in `globe.ts` `bind()` by adding a delegated click handler on `eg__rotate-btn-svg` that flips `toggle.checked` and dispatches `change`, with `e.stopPropagation()` so the click doesn't also start a drag operation on the figure. **(2) Drag-to-rotate correctness** — the JS `pointermove` handler applies `transform: rotate(N deg)` to `.eg__globe-rotating`; the CSS `transform-origin: 50% 50%` + `transform-box: view-box` on `.eg__globe-rotating` (lines 1280-1281) ensures the rotation pivots around the disc center. The horizontal-dx → 0.4-deg-per-px mapping is the natural-feel lever; vertical-dy maps to a 1.5%-scale zoom (down = zoom out, up = zoom in), clamped to [0.6, 2.4]. The `:hover` rule pauses auto-rotation, so the user can simply hover the figure to stop rotation, drag to spin, and walk away to resume. **Pause-button purpose** — the globe auto-rotates at 240s/revolution (visible from polar rings at lat ±66.5°). Without rotation, the disc looks like a static map and the polar rings lose their visual cue. The pause button exists for two reasons: (a) users on low-power devices or with `prefers-reduced-motion: reduce` get a stable view; (b) when reading event pins (hover callouts), the disc must stop so pins don't slide out from under the cursor. The 240s period was tuned so each continent gets ~equal screen time over time. `npm run no-bulbs` ✅, `npm run audit` ✅, build green at 84 pages in 2.0s. Live footer v6.10.51, deploy 446e589d.

**v6.10.50**: Final 5 chrome sites → `profile.stats.evalGates` + 6 cross-stat chrome sites across `profile.*`. The v6.10.49 sweep migrated 32 sites across 13 pages but missed these prose/KaTeX/aria sites that need a different surface pattern. `EntryStations.astro:277` Research station `flowOut: 'G1-31'` → `\`G1–G${profile.stats.evalGates}\`` (already imports profile from v6.10.40). `EvalConsole.astro:135` aria-label `\`Eval console — G1 through G31, ${counts.pass} pass…\``→`\`Eval console — G1 through G${profile.stats.evalGates}, ${counts.pass} pass…\``(already imports profile from v6.10.43).`skills.astro:74`hero KaTeX equation`\\mathrm{validate} = \\mathrm{G1\\;..\\;G31}`→`…\\mathrm{G1\\;..\\;G${profile.stats.evalGates}}` (added `import { profile } from '@utils/profile';`). `about.astro:60` ship-equation KaTeX `\\text{if } x \\text{ passes G1--G31}` → `…G1--G${profile.stats.evalGates}`(already imports profile).`about.astro:156`METHODOLOGY TEASER gate-family range`<strong>G23–G31</strong>`→`<strong>{\`G23–G${profile.stats.evalGates}\`}</strong>`. Total: 37 chrome sites now derive from `profile.stats.evalGates`— flipping the gate count from 31 to 32 flips all 37 simultaneously across prose, KaTeX equations, aria-labels, structured-data, and HTML chrome. The G1–G12 / G16–G20 ranges in`about.astro:154-155`are KEPT inline — those are structural gate-family range descriptors (not gate counts) that map to specific subset ranges in the methodology gate definitions. **Cross-stat chrome**: 6 additional sites migrated from hardcoded literals to`profile._`canonical sources —`Footer.astro:87`"Remote · 30 hrs/wk" →`Remote · {profile.contact.hours}`(already imports profile);`about.astro:243`"available 30 hrs/wk remote" →`available {profile.contact.hours} remote`(already imports profile);`about.astro:245`"built up in 11 months" →`built up in {profile.stats.ageMonths} months`(already imports profile);`certifications.astro:51`"collected in 11 months" →`collected in {profile.stats.ageMonths} months`(already imports profile);`about.astro:215`"Eleven agents in an orchestrator-worker topology" →`{profile.stats.aiAgentCount} agents in an orchestrator-worker topology`(already imports profile);`ai.astro:126`H2 "11 agents. A directed-acyclic graph…" →`{profile.stats.aiAgentCount} agents. A directed-acyclic graph…`(already imports profile);`index.astro:1017`H2 "What eleven months of build looks like." →`What {profile.stats.ageMonths} months of build looks like.`(already imports profile);`proof.astro:428`H2 "9 quant · 6 AI · 10 solutions." →`{quantProjects.length} quant · {aiProjects.length} AI · {solutions.length} solutions.`(already in scope from v6.10.27). That's 6 cross-stat chrome sites + the 5 final-gate-chrome sites = 11 sites flipped this version. Total cumulative chrome sites now deriving from`profile._`(since v6.10.32 sweep): 50+ across 18 files.`npm run no-bulbs`✅,`npm run audit` ✅, build green at 84 pages in 2.15s. Live footer v6.10.50, deploy hash pending.

**v6.10.49**: 32 chrome sites across 13 pages → `profile.stats.evalGates`. The largest single-source-of-truth sweep since v6.10.43 chrome unification — applies the same pattern to prose spread across the site. Each migrated site gains two module-level constants: `const GATE_RANGE = \`G1–G${profile.stats.evalGates}\`; const NGATE = \`${profile.stats.evalGates}-gate\`;`(immediately after`profile`import), then prose refs flip from`"…31-gate…"`/`"…G1–G31…"` literals to `` `…${NGATE}…` `` / `` `…${GATE_RANGE}…` `` template-literal interpolations. Sites: `methodology.astro`(7 — meta description, SectionTOC label, data-toc-label, H2 "31-gate…", step[2] desc, voice-line sub, methodology tag);`research.astro`(3 — marquee sub, voice-line sub, primaryLabel);`for-recruiters.astro`(1 — FAQ answer`a:`); `papers.astro`(2 — memo description + memo`method`); `mistakes.astro`(2 — prose "we run the standard {evalGates}-gate battery", voice-line sub; added profile import);`contact.astro`(2 — how-i-reply prose refs);`glossary.astro`(1 — CTABanner sub; added profile import);`talks.astro`(2 — lecture description + section sub);`proof.astro`(1 — VideoEmbed caption);`prediction-markets.astro`(1 — CTABanner sub; added profile import);`about.astro`(3 — METHODOLOGY TEASER blurb, H2 "31 gates before a number ships", prose "A 31-gate statistical evaluation");`ai.astro`(4 — loopSteps[2] desc, H1`<span class="hl">31 gates.</span>`, H2 + sub, ai-disclosure); `positions.astro`(1 — prose "31-gate eval harness, position-bias controls");`index.astro`(2 — marqueePool`'G1–G31 Gates'`+ shortCodes map key`'g1-g31'`). Total 32 chrome sites now derive from `profile.stats.evalGates`; flipping the gate count from 31 to 32 flips all 32 simultaneously. JSDoc comments in `EvalConsole`/`BrandMotif`/`Odometer`/`Stat`describing the gate stack are kept inline (JSDoc doesn't interpolate — developer-doc rule, not prose chrome).`npm run no-bulbs`✅,`npm run audit` ✅.

**v6.10.48**: Nav brand derivation + about.astro alt text. `Nav.astro:67-68` was the last hardcoded `Christian T. Macion, CTA®` + `Quant Researcher · AI Engineer` chrome on the site (visible in the top nav on every single page). Now derives from `profile.fullName` (with the `®` glyph wrapped in `<sup>` so the existing `.nav__brand-name sup` CSS rule still renders it as a proper typographic superscript at 0.65em in amber tint) + a new `profile.titles.short` field (intentionally distinct from `profile.titles.primary` — uses `Quant` not `Quantitative` so the word fits the 36px logo slot without ellipsis at the smallest desktop breakpoint; preserves the visual hierarchy at H1 size). `about.astro:42` avatar `alt` was hardcoded `Christian T. Macion, dark blazer over graphic tee, three-quarter view, neutral background.` — now derives from `profile.headshot.alt` (same string + adds `Photographed February 2026.` suffix for screen-reader context). All 3 nav-brand identity strings + 1 about-page alt text now share the same `profile.*` source of truth as the v6.10.47 feed + JSON-LD sweep. `npm run no-bulbs` ✅, `npm run audit` ✅, build green at 84 pages in 2.29s. Live footer v6.10.48, deploy hash pending.

**v6.10.47**: §9 determinism sweep + identity-string derivation. Atom feed `<updated>` timestamps (3 feeds: `feed.xml`, `feed-projects.xml`, `feed-solutions.xml`) — were `new Date().toISOString()` (wall-clock dependent; same source code, different rendered output every build). Now `buildStampUtc8Iso()` from `src/utils/build-stamp.ts` (re-introduced; knip had removed it in v6.10.28). Restored function: `const d = readBuildDate(); const shifted = new Date(d.getTime() + TZ_OFFSET_HOURS * 3600 * 1000); return shifted.toISOString();` — uses the same BUILD_DATE env-var pipeline as the Footer stamp. `_feed-shared.ts` PORTFOLIO_TAG_AUTHORITY: `new Date().getFullYear()` → `buildYear()` (same BUILD_DATE pipeline, deterministic per build). Identity strings — `Christian T. Macion` was hardcoded in 4 source files (`_feed-shared.ts` author `<name>` + `<email>`, `feed.xml.ts` + `feed-projects.xml.ts` + `feed-solutions.xml.ts` titles, `proof.astro` JSON-LD CollectionPage `name` + WebSite `name`, `seo.ts` `SITE_NAME` + description template). All migrated to `profile.fullName` / `profile.contact.email` / `profile.titles.primary` interpolations — single source of truth. Net result: `npm run deploy:mirror` on 2026-07-13 produces byte-identical feed `<updated>` + sitemap timestamps as the same deploy script run on 2026-08-13 — the build is reproducible from source. `npm run no-bulbs` ✅, `npm run audit` ✅ (0 NDA violations in 97 files), build green at 84 pages in 2.51s. Live footer v6.10.47, deploy hash pending.

**v6.10.46**: WorldFeed AI_POOL + AiEntryPoints GATES count derived. WorldFeed.astro:120 — `AI_POOL[0]` `'31-gate eval harness catches regression…'` → `\`${profile.stats.evalGates}-gate eval harness catches regression…\`` (template literal; profile import already present from v6.10.40). WorldFeed.astro:125 — `AI_POOL[5]` `'Eval harness gates G1 to G31 mechanical…'` → `\`Eval harness gates G1 to G${profile.stats.evalGates} mechanical…\``. AiEntryPoints.astro:46 — chip `count: 31`(numeric, used in`c.count.toString().padStart(2, '0')`for the`31 GATES`tile label) →`count: Number(profile.stats.evalGates)`. Added `import { profile } from '@utils/profile';`to AiEntryPoints frontmatter (after JSDoc, before type defs). Kept inline:`AI_POOL[1]` `"Agent roster revision: 11 nodes; orchestrator plus 4 inputs plus 3 middle plus 3 outputs"`— that's a structural decomposition where 1+4+3+3=11 matches the agent count, but if a 12th agent shipped the breakdown would need to be re-analyzed. The other 8 pool entries (RAG recall, structured-output pass-rate, MCP failure rate, JSON-schema validation, LLM judge agreement, prompt-injection probe, token budget) are kept — they're sampled-percentage/per-trace metrics with no canonical source. The 26 PAIRS / 15 PROJECTS chip counts in AiEntryPoints.astro:54,60 are kept inline — 26 = math-corpus pair count (no canonical), 15 = total projects (≥quant). Plus: AiEntryPoints JSDoc line 16 still says`"31 GATES / 26 PAIRS / 15 PROJECTS"`— JSDoc doesn't interpolate; kept for separate meta-doc sweep. 3 chrome sites flip when`evalGates` changes.

**v6.10.44**: `profile.titles.secondary` derived from canonical `stats`. The default `<meta description>` on every page that doesn't override, and the Person JSON-LD `description`, both come from `profile.titles.secondary`. Before this sweep, that string hardcoded `11-agent ... 31-gate ... 76.5k ... 102 certs` — the HIGHEST-leverage chrome string on the site (appears on every page's meta description, every page's OG card, every page's Twitter card, AND the JSON-LD Person record that Google's Knowledge Panel parses). Now `titles.secondary` is a template literal that interpolates `${stats.aiAgentCount} / ${stats.evalGates} / ${stats.locPython} / ${stats.certCount}` from the same canonical `stats` object exposed as `profile.stats`. Also migrated `awards[0]` — `"102 certificates (2024-12 → 2026-05)"` → `\`${stats.certCount} certificates (2024-12 → 2026-05)\``. Structural change: hoisted `stats`to a module-level`as const`block so it can be referenced by both`titles.secondary`(interpolated) and`profile.stats`(re-exported) — one canonical definition, two surfaces. Adding`stats.pagesBuilt` already propagates to home metric strip from v6.10.38.

**v6.10.43**: "11-agent / 31-gate / 102 certs" chrome unification — 12 chrome sites across 8 files migrated to `profile.stats.*`. `ai.astro:91-92` — title `AI lane — {aiAgentCount} agents, {evalGates} gates` and meta description `The {aiAgentCount}-agent platform and the {evalGates}-gate eval console behind every AI artifact…` (HIGHEST SEO impact — meta tag drives search snippet). `ai.astro:118` section `data-toc-label` derives. `positions.astro:253` role-card copy line `{aiAgentCount}-agent platform + {evalGates}-gate statistical eval harness`. `publications.astro:54` workbook scope `${aiAgentCount}-agent orchestrator…` (added profile import). `Footer.astro:75` every-page footer chrome `G1–G{evalGates} gate stack`. `AgentGraph.astro:60` DAG node label `G1–G${evalGates} · ${evalGates} gates` (added profile import). `EvalConsole.astro:138` console header `eval console · G1–G{evalGates} · 6 families` (added profile import). `about.astro:240` "I hold {certCount} professional certifications…". `BrandMotif.astro` aria-label (line 92) + SVG `<title>` (line 105) + SVG `<desc>` (line 107) all derive `G1–G{evalGates}` (added profile import). All 12 chrome sites now share the same canonical `profile.stats.*` source — flipping `evalGates` from "31" to "32" flips the footer, meta-tag, node label, console header, BrandMotif a11y labels, and Eval-Gate legend simultaneously.

**v6.10.42**: "nine" prose sweep — 10 hardcoded `nine`/`Nine` references replaced with `quantProjectCount` / `quantProjects.length` across 5 pages. `about.astro:171` H2 `"Nine projects, public-data reproducible."` → `{quantProjectCount} projects...`. `resume.astro:56` Quant Researcher Lane blurb `'nine public-data projects...'` → template literal with `${quantProjectCount}`. `papers.astro` — added `getCollection` import + `quantProjectCount` const, memo `method` line `'Run the standardized bootstrap-null routine on the nine /projects/quant strategies...'` → `\`...on the ${quantProjectCount} /projects/quant strategies...\``; memo `finding`line`'Three of the nine strategies clear the +1.65σ threshold...'`→ template literal.`mistakes.astro`— added import + const; JSDoc header`ships nine winners`+`<VoiceLine>` `A book of nine winners with no postmortems...`+`<p class="mistakes-head__sub">` `ships nine winners and hides the failure modes...`all derive.`projects/index.astro`—`across all nine is what makes the book interpretable to a risk manager`+`The book is not nine winning trades...`+`risk manager reading these nine projects sees the same engineering discipline`all interpolate`{quantProjects.length}`(already in scope from v6.10.29). The "nine NDA rules" reference in`about-this-site.astro:203`is **kept** — that's the nda-audit.ts rule count (1-9 in the file header), a curated count that maps directly to the source file. When the 10th quant project ships (raising`quantProjectCount` to 10), all 10 sites auto-update.

**v6.10.41**: F14 amber rgba migration to `color-mix`. 4 stale `rgba(196/201,138,22,*)` chrome refs replaced with `color-mix(in oklab, var(--c-amber) N%, transparent)` so future palette tweaks (F14 → F15) propagate without grepping every literal. Sites: `TrustStrip.astro:165` hover bg `0.06` → 6%; `EvalConsole.astro:321` check-row tint `0.04` → 4% (data semantics preserved); `skills.astro:363` mastery chip `0.14` → 14% (the +/- 5 R-channel drift from 196→201 at 14% alpha is invisible <0.7 RGB units rendered); `HeroVideo.astro:165` amber dots — inline `fill="rgba(201,138,22,0.22)"` removed from the `<g>` and migrated to CSS rule `.hero-video__dots { fill: color-mix(...) }` (SVG presentation-attr inheritance). The 13 `rgba(63,185,80,*)` / `rgba(248,81,73,*)` green/red pass/fail refs (OrderBook, EquityCurveChart regimes, HeroVideo cities, EvalConsole fail/pass pills, EarthFeed severity) are KEPT inline — these are functional data semantics (color encodes regime/severity/sentiment) under the chrome≠data rule. `npm run no-bulbs` ✅, `npm run audit` ✅, build green at 84 pages.

- Open-source content under `/proof` is public-data reproducible.
- v6.0.7 spine (additive on v6.0.6): **Million-dollar institutional UI/UX** — user directive was _"UI/UX still looks shit. focus on the UI/UX now. swarm, use as many agents, loops. use all the contexts to make it feel world class, institutional and professional. I hate how it looks. I want it like a million dollar level of website made."_ Five-hour budget. Three parallel research agents (institutional-reference synthesis across Jane Street / Man Group / Two Sigma / HRT / AQR / Schonfeld / Optiver / Numerai / HF / Replicate / Anthropic / Shreya / Neel / Olah / Gwern; visual diff current-vs-reference; current-site design-token inventory) produced a unified upgrade plan. Seven UI/UX changes shipped on the home page first: **(1) Section padding bumped from 64px to 128px desktop / 64px→96px mobile** — `.section` now uses `--sp-10` (8rem) on desktop and `--sp-8` on mobile. The single highest-leverage edit; reference sites use 96–160px. **(2) Italic-serif "voice line" anchor** — new `.voice-line` utility + `--serif-display` token stack (Iowan Old Style → Source Serif 4 → Georgia fallback). Home hero opens with "If you can't measure it, you haven't built it." in italic serif with amber quote marks + amber left rail. This is the Anthropic / Schonfeld signature move that distinguishes editorial-research sites from generic engineer portfolios. **(3) Oversized headline stats pulled above the fold** — `.stat--xl` now renders at `--fs-display` (clamp 56–92px, previously `--fs-4xl` = 48px). Home proof-band (102 certs · 31 gates · 76.5k LOC · 11 agents) is now the dominant numeric element on the page — the Two Sigma / Man / Schonfeld pattern of "we have receipts, flexed at scale." **(4) Manifesto section added below the proof band** — single italic-serif editorial sentence ("I ship the eval harness with the work — not slide decks about the work.") in a narrow centered column, with an uppercase tracked eyebrow ("— discipline, not theater") below. Generous `padding-block: clamp(sp-9, 10vw, sp-10)`. The Anthropic-style "moment of editorial weight" that re-anchors the page between the proof band and the process loop. **(5) `.section__sub` prose clamped to 65ch (~720px)** — even when the section itself uses the wide container, the prose intro below a heading now breathes in an editorial column. Anthropic / Shreya signature: prose should breathe, not stretch. **(6) Rotating statement carousel** — new `<StatementCarousel />` component replaces the static tagline pattern. Auto-advances every 5s, pauses on hover/focus, exposes dot-nav buttons for manual control, respects `prefers-reduced-motion`. Six rotating statements ("Eval-first. ↦ Ship-ready.", "NDA-clean. ↦ Public-data reproducible.", "31 gates. ↦ Zero rollbacks.", etc.) — the HRT signature: "innovators. engineers. technologists. mathematicians." Sits between the proof band and the manifesto. **(7) Tokenized z-index scale + tabular-numeral utility + serif-display stack** — `--z-base/raised/tape/sticky/nav/nav-more/modal/toast` (8 named layers), `--tnum` feature-settings variable + `.tnum` class, `--serif-display` font stack, `.eyebrow` utility for tracked-uppercase labels. Three foundational tokens let every component reference institutional-scale values instead of inlining them.

Then **the same pattern was rolled out across the rest of the site** so the institutional voice stays coherent — not just a home-page flourish: **(8) Italic-serif voice line on every page hero** — `/proof` ("If it isn't reproducible from public data, it isn't here."), `/methodology` ("If it isn't measured, it isn't built."), `/solutions` ("Every claim links to a falsifiable test. None of it relies on proprietary data."), `/certifications` ("Every cert links to its issuer's public registry — or it doesn't ship on this page."), `/about` ("I build the eval harness first. Then I build the work that has to pass it."), `/experience` ("Each engagement ships with proof. If the proof isn't public, the entry isn't here."), `/positions` ("Paper-trade first. Real capital only after the gates pass — never before."), `/now` ("Dated. Honest. Updated only when something changes that matters."), `/for-recruiters` ("60-second skim. NDA-clean. Built so a hiring manager can decide in one read."), `/projects` ("Every metric below is real, every method is documented, every line of code is on disk."), `/uses` ("A working researcher's stack — not a 2018-era ML engineer's. Updated quarterly."), `/reading` ("The re-reads matter more than the first reads."), `/publications` ("Dated. Linkable. If a row says I did something, the proof is in the same row."), `/papers` ("The five references /methodology is downstream of — and the working memos in flight."), `/talks` ("Every entry is on disk, named to the event, and linkable to the organizer."), `/mistakes` ("A book of nine winners with no postmortems is a book primed for selection bias."), `/colophon` ("Every design decision on this site, in one page. Tokens are the source of truth."), `/glossary` ("Twenty-five terms, each short enough to read in thirty seconds, each deep-linkable from /methodology."). Every page now leads with a single editorial sentence in italic serif, with amber quote marks and amber left rail — the same Anthropic / Schonfeld signature as the home page. **(9) Oversized stats row on `/proof`, `/methodology`, and `/projects` heroes** — `/proof` opens with a 22 / 9 / 6 / 10 stat band (artifacts on this page · quant projects · AI projects · case studies) at clamp 40–72px, separated by top + bottom hairline rules. `/methodology` opens with 31 / 6 / 11 / 0 (statistical gates · gate families · agents on the same method · rollbacks on shipped work). `/projects` opens with 15 / 6 / 9 / 15 (projects shipped · AI engineer projects · quant research projects · open-source repos). The "we have receipts, flexed at scale" pattern now reinforces the proof, method, and project pages, not just the home page. **(10) IntersectionObserver reveal-on-scroll hardened** — the original `[data-reveal]` CSS hid sections at opacity:0 until JS fired. On headless screenshots, slow connections, or any IO never-fire scenario, this left entire page sections invisible. Fixed by inverting the model: default state is visible, and only `:root.js [data-reveal]:not(.is-revealed)` is hidden. Plus a 1200ms safety-net in `reveal-on-scroll.ts` that force-reveals anything still hidden — late show is better than never. **(11) Flagship cert images now eager-load** — `ProofCard` gained an `eager` prop; `FlagshipCard` passes it through so the four Tier-1 cert portraits in `/proof` render immediately on page load instead of waiting for the user to scroll into view. 9 NDA rules pass 0 violations across 93 dist files.

- v6.0.6 spine (additive on v6.0.5): **ATS-friendly resume variants (PDF + Markdown + plain text)** — `/resume` previously served only the 3 tailored PDFs (CSS-rich, layout-dense). Most modern ATS pipelines (Workday classic, Greenhouse, Taleo, Lever) and LLM-based recruiter agents parse plain text and Markdown far more reliably than CSS-rich PDFs, where glyph subsets, table-for-layout positioning, and column splits routinely garble the extracted text. Fixed by adding `scripts/build-resumes-md.mjs`, which converts each `scripts/resumes/*.json` source to both `.md` (section headings + bullet lists + contact row, GitHub-renderable) and `.txt` (uppercase headings + bullet glyphs, ATS-friendly). 3 variants × 3 formats = 9 download artifacts now ship in `dist/` and on the mirror: `resume-{tailored,ai-only,quant-onepage}.{pdf,md,txt}`. The `/resume` page surface was rebuilt to show 3 cards × 3 format chips each (PDF = "visual · brand-faithful", MD = "LLM-agent · llms.txt ingest", TXT = "Workday · Greenhouse · Taleo"), and `/llms.txt` gained a dedicated "Resume artifacts" section so AI crawlers discover all 9 endpoints without parsing the page. The new builder is wired into `npm run prebuild` so every deploy ships the full set; `npm run resumes:build:md` runs the MD/TXT step alone for fast iteration. Headline stats on `/resume` bumped from `3 / 100% / 9 / 6` to `3 tailored / 3 formats / 100% NDA-safe / 9 quant proj` to surface the format count explicitly. 9 NDA rules pass 0 violations across 93 dist files.
- v6.0.5 spine (additive on v6.0.4): **eight site improvements** (all discovered by v6.0.4 post-deploy audit + v6.0.5 deployment) — **(1) Atom feed URL base path missing** — `feed.xml` / `feed-projects.xml` / `feed-solutions.xml` entry URLs were pointed at `https://christianmacion26.github.io/solutions/` instead of `https://christianmacion26.github.io/portfolio/solutions/`. Root cause: feed scripts computed `baseUrl` from `site?.toString()` only. Fixed by combining with `import.meta.env.BASE_URL` so feed entry URLs match the canonical sitemap and `<link rel="canonical">` URLs. **(2) AI research pages showed slug as H1** — `/research/ai/{slug}` H1s rendered `rag-recall`, `toolcall-agent`, `judge-harness`, `eval-mcp-server`, `reflect-revise`, `slop-scanner` (filenames) instead of human titles. Fixed by updating `title:` in each `src/content/projects/ai/*.mdx` frontmatter to: RAG Recall Eval · Tool-Call Agent · LLM-as-Judge Harness · Eval MCP Server · Reflect-Revise Loop · Slop Scanner. Quant pages were already correct. **(3) /methodology cross-link desert** — page mentioned 13 glossary terms (DSR, PBO, CSCV, walk-forward, block-bootstrap, Bonferroni, MinBTL, slippage, embargo, OOS, Sharpe, regime, survivorship) but **zero** inbound links to `/glossary/{id}` deep pages. Discovery was gated on the nav click alone. Fixed by wrapping strategic first-mentions with `<a href={path("/glossary/{id}")}>` in 12 spots across the gate stack, H2, "If you hired me" plan, and negative-space section. **12 unique glossary IDs now receive inbound links from /methodology** (DSR, PBO, walk-forward, block-bootstrap, embargo, OOS, slippage, Sharpe, regime, Bonferroni-Holm, MinBTL, survivorship-bias — up from zero). **(4) /publications 6-month gap** — latest entry was 2026-01-19 (CTA cert), creating a "not shipping publicly right now" read for a Q2/Q3 skim. Fixed by adding 4 new press entries: v6.0.5 site patch (today), v6.0.4 spine notes (today), live paper-trade on /positions (2026-04-21), slop-scanner app release (2026-03-22). Latest entry now dated 2026-07-10. Press list also gained a stable date-desc sort (was rendering in literal source order, which got the year column wrong after the prepend). **(5) /now page compression** — "Not open to" section had 5 bullets that duplicated /positions "Off the table" content. Compressed to 2 bullets that redirect to /for-recruiters and /contact. "Currently reading" list expanded from 3 to 4 bullets with a 2024 NLP paper. **(6) Homepage H1 casing** — H1 said "quantitative researcher" (sentence case) but `profile.titles.primary` is "Quantitative Researcher" (title case). Fixed to match the canonical title. **(7) CF Pages mirror `build:mirror` script** — bare `wrangler` was not on PATH (only `npx wrangler` works), and the postbuild chain was missing `asset-audit` + `pagefind` index steps (so the mirror served without site search). Fixed by re-writing the chain as `PUBLIC_SITE_URL=https://christianmacion-portfolio.pages.dev astro build --base=/ && node scripts/build-llms-full.mjs && cp public/llms-full.txt dist/llms-full.txt && node scripts/asset-audit.mjs && pagefind --site dist --output-subdir _pagefind`. Mirror now serves `/_pagefind/pagefind.js` → 200 and `wrangler` resolves via `npx`. **(8) Mirror host leak across feeds + sitemap + canonical + og:url** — after fix #7, every generated XML artifact (atom feed self-link + 50 entry links, sitemap-index.xml + 80 sitemap-0.xml `<loc>` entries) and every `<link rel="canonical">` / `<meta property="og:url">` / JSON-LD `url` on the mirror build still pointed at `https://christianmacion26.github.io` because `astro.config.mjs` had `site: 'https://christianmacion26.github.io'` hardcoded (and `src/utils/seo.ts` had a separate `SITE_URL` constant). Fixed by reading both from `process.env.PUBLIC_SITE_URL ?? 'https://christianmacion26.github.io'` so the mirror build sets `PUBLIC_SITE_URL=https://christianmacion-portfolio.pages.dev` once and all generated artifacts follow. **Important**: GH Pages CDN edge on .111.153 is documented as broken in PH/sandbox region (the canonical deployment the host was leaking to). This bug actively broke RSS reader subscribers, AI recruiter agents pulling `sitemap.xml`, and PH/sandbox visitors following canonical links — exactly the region the site is positioned for (OnlineJobs.ph, CHED, etc.). 9 NDA rules pass 0 violations across 87 dist files.
- v6.0.4 spine (additive on v6.0.3): 25 new `/glossary/[id]` deep pages (one per term in `src/data/glossary-terms.ts`, each with extended prose + related-term cross-links), 15 new `/research/[...slug]` academic-citation pages (one per quant and AI project with auto-generated BibTeX entry + copy-to-clipboard button + canonical-link row to `/projects/[id]`), `/proof` FlagshipCard self-link bug fixed (3 cert files now point to issuer URLs — ateneo.edu, sta-uk.com, bida.gov.ph, plus bitget.com for the orphaned B4Y cert), Numerai row in Public Track Record now reads "not public yet" with `ptr__cell--empty` italic styling and links to `/positions`, `/proof` page version stamp updated from `v5.3` to `v6.0.3`, all build-time dates switched from UTC to UTC+8 (Philippine local time) so the Footer "last deployed" stamp doesn't roll back by a day on overnight builds. Total 80 pages indexed. 9 NDA rules pass 0 violations across 87 dist files.
- v6.0.3 spine (additive on v6.0.2): P0/P1 visual fixes shipped (proof TOC items now build-time rendered, not JS-discovery · nav CTA always amber-on-tape · btn--primary/ghost now amber instead of cream-on-cream · stat-tile corner glyphs removed · /publications topic chips now amber-active · H1 yellow underline bleed past period fixed at CSS + markup layer · CTABanner amber-on-tape · mobile facts table clamp to 1fr / overflow-wrap anywhere · mobile KaTeX scaled 0.74em + horizontal-scroll-safe · /glossary `[slug]` deep pages TODO · 7 `data-toc-id` markers present across /proof / methodology / use · BreadcrumbList JSON-LD wired into 23 pages · new `/llms-full.txt` (155 KB / 21 pages / 3,706 lines) auto-generated by `scripts/build-llms-full.mjs` and wired into `npm run build` post-astro). 9 NDA rules pass 0 violations across 47 dist files.
- v6.0.2 spine (additive on v6.0.1): `/positions` rebuilt as a live book (status banner with green-dot pulse · 5 target-role cards CONVERSATION 01–05 with "Looking for / I'm a fit because" + ghost "Next step" CTAs · Calendly strip with 30-min intro-call CTA · "Off the table" hard-nos via ✕ bullets), `/proof` rebuilt as a 6-section narrative with two-axis filter bar (Topic × Type chips with URL query sync) · status pills + read-time pills on every row · new P.5b "Public track record" 6-row table · per-list pagination (6/page · pinned-first · page chips rendered by JS), home hero gets a 1-line pitchbook TL;DR ("Quant researcher + AI engineer who builds eval-first research systems and ships the evals with them") + a 7-chip "Talk to me about" topic-anchor strip, `/methodology` wires the `PipelineDiagram` SVG into the routing section so the G1–G31 gate stack shows as a real control-systems loop with feedback curve, footer bumps to `portfolio.os · v6.0.2 · last deployed YYYY-MM-DD · built with Astro`. 9 NDA rules pass 0 violations across 46 dist files.
- v5.2 spine: identity-first home hero (REMOTE-READY eyebrow · third-person H1 · cert portrait + headshot 2-up · text-link CTAs · ticker moved below hero), `/proof` rebuilt as a 6-section narrative (P.1 identity · P.2 4 flagship certs · P.3 4 CodeProofCards · P.4 9 quant + 6 AI + 10 solutions lists · P.5 videos + chart + RSS · P.6 disclosure), institutional motion vocabulary layered (odometer counter on `data-counter` · active-nav via IntersectionObserver · FlagshipCard shine sweep · code-block copy-to-clipboard · ticker ease clamp 60s→45s with cubic-bezier · section fade-in on scroll · verified-dot pulse 2.4s). All motion gated by `prefers-reduced-motion`. Script bundle <4KB gzipped.

---

_This file is the source of truth for AI-recruiter indexing. Last edited 2026-07-14 · v6.11.18 (F14 CI gate expanded to scan Astro-inlined HTML + raw rgba green/red literals; 6 surviving F14 violations swept across Stat/Chart/EarthPulse/EquityCurveChart/HeroVideo/copy-button; 11 chrome derivation sites across 9 files. Reproducible test covers 3 violation modes (CSS var, HTML inlined var, raw rgba). All 9 CI gates green. Deploy `351f2e27`. Additive on v6.0.11 full-dark palette.)_

*Recent polish (2026-07-13, v6.10.17-v6.10.35): 33 decorative amber hero radial-gradients killed across 9 secondary pages + dynamic-route pages (chrome≠data rule). --c-ink-3 lifted from #6e7681 → #8b949e for WCAG AA contrast (5.6:1). NavMore role="menu" ARIA anti-pattern removed. Publications filter radios no longer aria-hidden. 404 page noindex meta tag now reaches <head> via named <slot name="head" />. CLS penalty on 9 project hero imgs fixed with width/height attrs (matches 16:9 aspect-ratio). 7 page titles deduped (template literal + buildMeta double-appending fixed). OG image footer URL now reads canonical `christianmacion-portfolio.pages.dev` instead of stale `christianmacion26.github.io/portfolio`. Killed duplicate `.hero__glow` div on home (same grid pattern as `.hero__grid` — 2 DOM nodes + 2 CSS rules for zero visual delta). Killed empty `.r-hero__glow` div on /research (background: line was dropped in v6.9.16 but the absolute-positioned empty box remained). `/proof` hero stats now derive from collections (live: 25 / 9 / 6 / 10 — was hardcoded 22 / 9 / 6 / 10). Dead-code cleanup: -429 LOC (ProcessStrip + WorldEventMap components deleted, unused buildStampUtc8Iso + driftWalk removed). `humans.md` deduplicated: killed 3 stale Version lines (v6.0.7c / v6.0.7d / v6.1.0 were left over from prior linter runs), footer version stamp corrected v6.0.7 → v6.10.27, linter typo v6.10.27.25 → v6.10.27. **v6.10.28**: 4-fam Hemisphere lat-band grouping for `EarthGlobe.astro` continent paths (240 inline `fill="rgb(...)"` attrs → 1 parent `<g fill>` ancestor × 4 LAT_RAMP stops — SVG presentation-attr inheritance). Also `scripts/build-earth-110m.mjs` path-data minification: drop the space after each command letter (`l 17.8` → `l17.8`) and the inter-subpath separator space (`Z M` → `ZM`); 4 continents × ~37KB → ~30KB. **v6.10.29**: `/projects` page — "open-source repos" stat (was hand-written `15`) now derives from `projects.filter(p => p.data.sourceLink).length`; "What 9 projects tell you about a research book" H2 now reads `{quantProjects.length} projects`; killed the last decorative hero amber radial-gradient on /projects (chrome≠data rule, this one survived the v6.10.18 sweep). **v6.10.30**: `HeroVideo.astro` SVG chrome restraint — 8 pulsing city markers now hoist their identical `fill="rgba(63, 185, 80, 0.55)"` to the parent `<g class="hero-video__cities">` (SVG presentation-attr inheritance); 8 inline attrs → 1 parent attr (-258 B home). Orbits attempt with mixed amber/cyan `fill-opacity` was reverted: `fill-opacity="..."` attr is _longer_ than the `fill="rgba(...)"` it replaces when only some children match the dominant pattern. **v6.10.31**: `EarthGlobe.astro` `buildLatGrid` + `buildLonGrid` apply the v6.10.28 continent-path minification (drop space after `M`/`L` letter + drop inter-segment separator); `<g class="eg__grid">` 6,731 → 5,883 B (-848 B) and polar-rings 1,141 → 993 B (-148 B). Total home 606,785 → 605,683 B (-1,102 B / -0.18%); `/markets` should show similar gain since it renders the same EarthGlobe with full grid. A/B build confirmed. **Also `/colophon`** — the design-system reference page had stale F14-era values: swatch hex `'#e8b220'` → `'#c98a16'` (matches live `--c-amber` token), contrast row `--c-amber` rated `3.4:1 AA Large` → `6.4:1 AA` (computed via WCAG 2.1 relative luminance — the new amber is 1.9× more readable than the old claim), description "decorative amber tokens reserved for ≥18px / non-text" tightened to "`--c-amber-light` hover variant reserved for ≥18px / non-text use on cream surfaces (drops to 1.7:1 there)" — `tokens.css` tokens unchanged. 9 NDA rules pass 0 violations across 97 dist files. Live ~606 KB home (down from ~624 KB), 84 pages. **v6.10.32**: 14 hardcoded numeric claims replaced with `profile.stats.*` interpolations (or collection-derived counts) across 7 pages — `/ai` (11/31/6/76.5k), `/methodology` (31 from `gates.reduce(...)`, 6 from `gates.length`; 11 agents + 0 rollbacks stay hardcoded — no canonical source), `/about` (9 quant projects via `projects.filter(role==='quant').length`), `/for-recruiters` (FAQ + Stack row), `/solutions` (4 Stat values), `/uses` (Python entry `why:` → template literal), `/positions` (2 role-card copy lines). Zero cosmetic change — same numbers in the rendered output, single source of truth = `profile.stats.{aiAgentCount, evalGates, locPython, certCount}`. AI-recruiter cross-page consistency now guaranteed at the type level. **v6.10.33**: stale F13-era amber gradient killed on 3 "flagship/primary" card variants — `certifications.astro` (`.cert--flagship`), `resume.astro` (`.resume-card--primary`), `contact.astro` (`.contact-card--primary`). All three were rendering `background: linear-gradient(180deg, rgba(212, 160, 23, 0.04), var(--c-bg))` — a stale F13 pre-token amber tint with hardcoded `rgba(212, 160, 23, ...)` (current `--c-amber` is `#c98a16` = rgb(201,138,22)). Replaced with `background: var(--c-bg-2)` token-derived lift; each variant's existing border-color signal is preserved (amber-deep on cert; off-white on resume/contact). chrome≠data rule honored — no decorative gradient left; the amber-deep border remains the only chrome indicator. `npm run no-bulbs` ✅, `npm run audit` ✅ (0 NDA violations in 97 files), build green at 84 pages in 2.92s. Live footer v6.10.33, deploy hash `a5b4d4ea`. **v6.10.34**: 10 remaining hardcoded numeric claims replaced with derivations across 3 pages. `/research` — stat values `'31'` → `profile.stats.evalGates`, `'102'` → `profile.stats.certCount`; H1 `"Nine strategies. Four factors. Twelve sources."` → `{quantProjects.length} strategies...`; eyebrow `"All nine strategies"` → `{`All ${quantProjects.length} strategies`}`; description derives 9 strategies from `quantProjects.length`. No-op linear-gradient `.r-disclose` (linear-gradient `var(--c-bg)` → `var(--c-tape)` where both were `#0d1117`) replaced with solid `var(--c-bg)` fill. `/proof` — description derives from `${codeArtifacts.length}/${quantProjects.length}/${aiProjects.length}/${solutions.length}` (4/9/6/10 today); JSON-LD `hasPart` code entry joins `codeArtifacts.map(c => c.title)`; JSON-LD research entry counts same way. `/skills` — `sub="three"` (label, not value) now derives from `groups.length` via inline conditional (one/two/three/fallback); today's `groups.length === 3` so rendered text is the same, but a future 4th group will auto-update. `npm run no-bulbs` ✅, `npm run audit` ✅, build green at 84 pages in 4.46s after cache wipe. Live footer v6.10.34, deploy hash `d31357f7`. **v6.10.35**: `/resume` 5 hardcoded numerics derived. Added `getCollection('project')` import + `quantProjectCount` const. `resumeCount = resumes.length` (today 3), `formatCount = resumes[0]?.formats?.length ?? 0` (today 3), `downloadCount = resumeCount * formatCount`. Meta description: `"Three tailored resumes × 3 formats (PDF / Markdown / plain text)..."` → template literal. Hero eyebrow `"3 versions × 3 formats = 9 download targets"` → expression. H1 `"Three tailored resumes."` → conditional `'Three tailored resumes.'` (3) / `'{n} tailored resumes.'` (other). 4-stat row values: `"3"/"3"/"9"` → `{String(resumeCount)}/{String(formatCount)}/{String(quantProjectCount)}`; `"100%"` kept (NDA-safe is a claim, not a count). `npm run no-bulbs` ✅, `npm run audit` ✅, build green at 84 pages in 3.73s. Live footer v6.10.35, deploy hash `0f6991fa`. **v6.10.36**: F13 amber token consolidation. 7 stale `rgba(212, 160, 23, …)` chrome refs replaced with new token aliases `--c-amber-soft` (0.12, 5 sites: proof.astro ×4 + publications.astro ×1), `--c-amber-decor-deep` (0.3, 1 site: global.css `.pill--accent` border), `--c-amber-faint` (0.05, 1 site: skills.astro `.skill__chip--lvl-3`). global.css `.pill--accent` bg also normalizes 0.1 → `--c-amber-soft` (sub-perceptual delta, consistent with 4 other active-bg sites). 3 inline rgba(212) refs KEPT — `EquityCurveChart.astro` regime fill (functional data viz, alpha-mixed with calm `rgba(63,185,80,0.06)` + stress `rgba(248,81,73,0.07)`) and `search.astro` pagefind `<mark>` highlight. `tokens.css` itself still declares the source `rgba(212,160,23,…)` family at the top (`--c-amber-decor` + 3 new aliases) so a future F14 color refresh changes one place. `npm run no-bulbs` ✅, `npm run audit` ✅ (0 NDA violations in 97 files). **v6.10.37**: `/positions` derived `tradingDays = equitySeries.length` (50 today). Moved the inline `data={[…]}` array on `<EquityCurveChart>` into frontmatter `const equitySeries = […]`. Copy line 78 `"50 trading days"` and line 90 chart-sub `"12% return over 50 trading days"` both now use `{tradingDays}`. Net return (12%) stays curated — pairs with terminal chart point + IS backtest claim. When the live series is extended to 60/70 days, the page reports correctly without manual copy edits. **v6.10.38**: home metric strip 2 hardcoded numerics derived. `metricFacts[0] PAGES BUILT` `'84'` → `String(profile.stats.pagesBuilt)` (new `pagesBuilt: '84'` stat added to profile.ts — canonical page count, updated only on deliberate route add/remove). `metricFacts[4] STRATEGIES` `'9'` → `String(quantCount)` (line 59 const: `projects.filter(p => p.data.role === 'quant').length`). Other 6 metric facts already derived (EVAL GATES / AI AGENTS / CERTS / MONTHS via `profile.stats.*`; AUDIT '100%' + AFK LOOP '6H' are curated process claims, kept hardcoded with rationale). Build-time comment blocks updated to reference `profile.stats.pagesBuilt` instead of literal 84. **v6.10.39**: 3 more derivations across 2 files. `StatementCarousel.astro` defaults (`'31 gates. ↦ Zero rollbacks.' / '11 agents. ↦ One method.' / '102 certs. ↦ One discipline.'`) now interpolate from `profile.stats.{evalGates, aiAgentCount, certCount}` — dead code in production (all 4 callers pass their own `statements` prop) but kept for grep consistency and graceful fallback. `index.astro` `<StatementCarousel variant="graphic" statements={[...]}>` line `'31 gates. ↦ Zero rollbacks.'` also derives. `AgentGraph.astro` aria-label + legend caption (`11 agents · 4 inputs → orchestrator → 3 middles → 3 outputs · feedback loops to orchestrator`) now derive from `nodes.filter(family === 'input'|'middle'|'output').length` plus `nodes.length` — adding a 5th input node to the DAG auto-updates the descriptor, instead of leaving stale "4 inputs" copy. **v6.10.40**: EntryStations + WorldFeed + /proof GitHub table derive 6 more strings. `EntryStations.astro` Research station (line 263) `numHtml: '9'` → `String(quantCount)`; caption `'strategies · G1–G31'` → `\`strategies · G1–G${profile.stats.evalGates}\``; stats `['gates', 31]`→`parseInt(profile.stats.evalGates)`. Evals station caption `'eval pass rate · 31 gates'`→`\`eval pass rate · ${profile.stats.evalGates} gates\``. `WorldFeed.astro:134` AI/eval fixture pool line `'Agent-fleet harness regression suite: 12 of 31 gates auto-triggered'` → template literal `\`...12 of ${profile.stats.evalGates} gates auto-triggered\``. `/proof`GitHub table row (line 730)`'102 certs · 76.5k LOC · 15 OSS projects'`→`{profile.stats.certCount} certs · {profile.stats.locPython} LOC · {ossProjectCount} OSS projects`where`ossProjectCount = projects.filter(p => p.data.sourceLink).length`. *
